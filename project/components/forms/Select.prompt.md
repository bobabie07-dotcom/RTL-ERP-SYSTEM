Dropdown filter — the ERP's farm/date/category/status selectors live in headers and toolbars.

```jsx
<Select label="Selected Farm" options={['RTL Main Farm','RTL North Farm','All Farms']} />
<Select options={[{value:'all',label:'All Status'},{value:'active',label:'Active'}]} />
```
