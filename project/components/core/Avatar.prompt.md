User avatar — image when available, otherwise initials on a green tint. Add `showText` for the top-bar account block.

```jsx
<Avatar name="Admin User" />
<Avatar src="/me.jpg" name="Admin User" role="Farm Manager" showText />
```
