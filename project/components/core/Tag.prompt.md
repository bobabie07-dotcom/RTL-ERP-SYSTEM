Quiet neutral chip for categories and filter selections — lighter than a semantic Badge.

```jsx
<Tag>Feed</Tag>
<Tag color="green">Starter Mash</Tag>
<Tag onRemove={() => removeFilter('rtl-main')}>RTL Main Farm</Tag>
```
